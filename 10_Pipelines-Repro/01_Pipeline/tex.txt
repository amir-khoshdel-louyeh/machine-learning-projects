Title: Project 1 — Build reusable ML pipeline

Tasks:
- Build a pipeline for data ingestion, preprocessing, feature engineering, model training and evaluation
- Serialize model and pipeline (joblib)
- Set seeds, environment capture (requirements.txt)

Deliverable:
- Reproducible notebook and Python package/module with CLI for training/prediction

Acceptance:
- One command reproduces key results on same data split
